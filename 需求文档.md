# AI 求职助手网站 — 产品需求文档（PRD）

## 1. 产品概述

### 1.1 产品背景
求职者在投递不同岗位时，往往需要针对每个目标岗位调整简历、撰写求职信、准备面试。手工完成这些工作耗时且质量不稳定。本产品通过 DeepSeek AI，根据目标岗位描述对求职者的简历进行智能分析，输出匹配度评估、优化建议、求职信和面试题参考回答，帮助求职者快速、有针对性地优化求职材料，提升投递效率与面试准备质量。

### 1.2 产品定位
一款面向求职者的 AI 求职助手网站。用户上传简历 PDF 并粘贴目标岗位描述，系统自动提取简历文字，调用 DeepSeek API 生成结构化的求职材料分析结果。

### 1.3 目标用户
- 主要用户：中文求职者（岗位描述可为中文或英文）。
- 典型场景：准备投递简历前，针对某一具体岗位 JD 快速评估匹配度、优化简历、生成求职信、准备面试。

### 1.4 核心价值
- **快速**：一次上传 + 一次粘贴，短时间内获得完整分析。
- **针对性**：所有输出均围绕目标岗位生成，而非通用模板。
- **可操作**：优化建议具体可执行，求职信可直接复制使用。

### 1.5 产品目标（v1）
- 完成从「上传简历 + 粘贴岗位描述」到「展示四类分析结果」的完整闭环。
- 保证 DeepSeek API Key 安全，不暴露于前端。
- 第一版不引入数据库，分析结果仅保存在当前页面状态。

## 2. 用户流程

1. 用户进入首页。
2. 上传自己的简历 PDF（拖拽或点击选择）。
3. 粘贴目标岗位描述。
4. 系统在前端提取 PDF 中的简历文字。
5. 系统将「简历文字 + 岗位描述」发送给服务端 API。
6. 服务端调用 DeepSeek API，返回结构化分析结果。
7. 页面分四个板块展示：岗位匹配度分析、简历优化建议、求职信、10 个面试问题及参考回答。
8. 用户可一键复制求职信 / 问答内容；可修改输入后重新生成。

## 3. 功能需求

### F1 简历上传
- 支持 PDF 格式，通过拖拽或点击文件选择器上传。
- 文件校验：非 PDF 文件给出错误提示并拒绝处理。
- 大小限制：默认上限 10MB，超出时提示用户。
- 上传后展示文件名与解析状态（解析中 / 解析成功 / 解析失败）。
- 解析失败（如无文字层的扫描件）时给出明确提示，允许用户更换文件。

### F2 岗位描述输入
- 文本域粘贴输入，必填。
- 未填写时禁止提交并提示。
- 超长限制：岗位描述超过约定长度（默认 8000 字符）时截断或提示，避免超出模型上下文。

### F3 PDF 文字提取
- 使用前端库 `pdfjs-dist` 在浏览器端提取 PDF 文字。
- 提取过程异步执行，不阻塞页面；大文件显示加载状态。
- 当提取到的文字过少（少于 30 字符）时，自动判定为扫描件/图片型 PDF，并在浏览器端用 `tesseract.js`（WASM，中文 `chi_sim` + 英文 `eng`）逐页 OCR 识别，识别文字继续进入后续流程。
- OCR 资源（worker、core wasm、语言包）自托管于 `public/tessdata/`，支持离线与国内网络；OCR 最多处理前 10 页，单页渲染长边上限 4096px。
- 提取成功（含 OCR 成功）后展示简历文字预览（可选，显示前若干字符），确认后进入分析。

### F4 AI 分析调用
- 前端将简历文字与岗位描述通过 POST 发送到服务端 API Route `/api/analyze`。
- 服务端校验输入非空、长度合理，再调用 DeepSeek API。
- 默认模型 `deepseek-v4-flash`，可通过环境变量 `DEEPSEEK_MODEL` 覆盖。
- 非流式、单次请求，一次返回全部四类内容。
- 超时、限流、API 错误时返回统一错误格式，前端展示可重试提示。

### F5 结果展示（四个板块）
1. **岗位匹配度分析**：总匹配度评分（百分比）+ 分维度评分（技能、经验、学历 / 资质等）+ 文字匹配分析与差距说明。
2. **简历优化建议**：分类、可操作、按优先级排序的具体建议（如补充关键词、量化成果、调整表述、补充技能）。
3. **求职信**：一封完整、可直接复制的求职信，紧扣岗位描述与简历亮点。
4. **10 个面试问题及参考回答**：针对该岗位的 10 个问题，每个问题附参考回答。

### F6 加载与错误处理
- 分析中展示加载状态（骨架屏 / 进度提示），禁止重复提交。
- 错误类型：PDF 解析失败、输入校验失败、网络错误、AI API 错误、结果解析失败。
- 所有错误均提供用户可理解的中文提示与重试按钮。
- 结果解析失败时兜底展示 AI 返回的原始文本。

### F7 复制与重新生成
- 求职信、单个面试问答支持一键复制。
- 用户可修改简历 / 岗位描述后重新生成（覆盖当前结果）。
- 刷新页面后结果清空（结果仅存页面状态，无持久化）。

## 4. 页面与交互

- 单页应用，三步式布局：
  1. **输入区**：简历上传 + 岗位描述输入 + 「开始分析」按钮。
  2. **分析中**：加载状态。
  3. **结果区**：四个板块卡片展示。
- 结果区懒渲染；匹配度评分用进度条 / 环形图直观展示。
- 页面包含隐私提示：「您的简历内容仅用于本次分析，不会被存储」。
- 响应式：桌面端与移动端均可正常使用。

## 5. 技术方案

### 5.1 技术栈
- 前端框架：Next.js（App Router）+ TypeScript
- 样式与组件：Tailwind CSS + shadcn/ui
- PDF 提取：`pdfjs-dist`（前端）
- AI 服务：DeepSeek API

### 5.2 架构
- **前端**：上传 PDF → `pdfjs-dist` 提取文字 → POST `/api/analyze` → 渲染结果。
- **服务端**：Next.js API Route `/api/analyze` 接收请求 → 校验输入 → 调用 DeepSeek → 返回结构化 JSON。
- 不引入数据库、无登录体系。

### 5.3 环境变量
| 变量 | 说明 | 默认值 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | DeepSeek API Key（必填） | 无 |
| `DEEPSEEK_MODEL` | 模型名称 | `deepseek-v4-flash` |

- Key 仅存在于服务端环境（`.env.local` / 部署平台环境变量），绝不写入前端代码或 `NEXT_PUBLIC_` 前缀变量。

### 5.4 AI 输出约定
- 单次请求，提示词要求返回严格 JSON，结构约定如下（字段名可按实现微调）：
  - `matchAnalysis`：`{ totalScore, dimensions: [{ key, name, score, comment }], summary, gapAnalysis }`（`key` 取值 `skill | experience | education | overall`，固定 4 维度）
- 评分稳定性：固定 4 维度 + 分档锚点（90-100 / 75-89 / 60-74 / 40-59 / 0-39）；总分由服务端按权重（技能 35%、经验 35%、学历资质 15%、整体 15%）加权计算，不依赖模型自报；采样 `temperature=0.2` 并按输入内容派生固定 `seed`，同输入重复分析评分基本一致。
  - `resumeSuggestions`：`[{ category, priority, suggestion }]`
  - `coverLetter`：`string`
  - `interviewQuestions`：`[{ question, referenceAnswer }]`（共 10 条）
- 输出语言：跟随岗位描述语言（描述为英文则输出英文，描述为中文则输出中文）。
- 前端解析 JSON 渲染；解析失败时展示原始文本兜底。

### 5.5 提示词要点
- 明确 AI 角色：资深求职顾问 / HR 视角。
- 要求紧扣岗位描述中的关键词、职责与任职要求。
- 优化建议必须具体、可操作，避免空泛。
- 求职信篇幅适中（约 300–500 字），突出简历与岗位的匹配亮点。
- 面试题结合岗位职责与简历经历，参考回答给出结构化要点。

## 6. 非功能需求

- **安全**：API Key 不暴露；简历数据不落库、不缓存，仅在请求处理期存在于内存。
- **性能**：PDF 提取异步化；结果区懒渲染；请求超时时间合理设置。
- **可用性**：加载与错误状态完备；移动端适配。
- **可维护性**：代码分层清晰（页面、API、AI 服务、类型定义）；AI 输出结构有类型定义与校验。

## 7. 测试与验收标准

| 场景 | 预期结果 |
| --- | --- |
| 上传非 PDF 文件 | 拒绝并提示「仅支持 PDF 格式」 |
| 上传超过 10MB 的 PDF | 拒绝并提示文件过大 |
| 未填写岗位描述点击分析 | 禁止提交并提示必填 |
| 正常上传有效 PDF + 填写岗位描述 | 四个板块完整展示，含总评分与分维度评分 |
| DeepSeek API 失败 / 网络异常 | 展示可重试的错误提示 |
| AI 返回非预期格式 | 兜底展示原始文本，不白屏 |
| 刷新页面 | 结果清空，回到初始状态 |
| 点击复制 | 求职信 / 问答复制成功 |
| 移动端浏览 | 布局正常、可操作 |

## 8. 版本范围与后续迭代

### v1 明确不做
- 数据库、用户登录、历史记录
- 文件导出（Word / PDF）
- 流式输出
- 多轮对话

### 后续可选方向
- 历史记录与多轮对话
- 导出 Word / PDF
- 简历模板与优化建议「一键应用」
- 多模型可选
